'use client';

// TODO: Sam widget (custom) — replace with actual script once trysam.co tenant is onboarded
// This is NOT Chatbase. This is Sam's own custom widget, pending deployment.

export default function SamWidget() {
  // Widget script will be injected here once the trysam.co tenant is onboarded
  // Example: <Script src="https://widget.trysam.co/embed.js" data-tenant="trysam" strategy="lazyOnload" />
  return null;
}
